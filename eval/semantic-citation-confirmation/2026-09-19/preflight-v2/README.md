# Two-query confirmation — successor preflight v2

**GO pending explicit live approval and an immediately-before-launch binding check.** This is an
offline preparation packet, not approval or an executed model run. The prior no-go freeze remains
unchanged at `f98cab8947112698dc2e8d58ba30441dc6d8f40f3d1a35125994ce50a34e7643` (760 members).

Exactly `total`, then `period_groups`. Query bytes, model-facing payloads and source-reviewed evaluator
expectations are unchanged. `total` requires immediate 合計 plus 租賃(HKFRS 16) and revenue-group
support. `period_groups` requires the three target-2025 peers and the 2025 group, excluding 2024 peers.
The literal 2026 caption and common display units remain. Fresh role requests bind attempt 1.2.0 and
nonce **live-v2**, with distinct role identities. Operation 1.1.0 and analysis 1.0.1 are unchanged.

Reserved output, **nonexistent and unused**:
`runs/semantic-citation-confirmation-2026-09-19/live-v2`.
The exact argv is in [launch-proposal.json](launch-proposal.json). Ceilings remain exactly six HTTP
calls (two metadata GETs, two primary plus two research completions), 600 seconds total, 60 seconds per
attempt, $8 conservative reservation, 4096 tokens per completion, zero retries/fallback. Provider
availability/pricing remain unknown; any authorized metadata checks consume this same budget.
Failure permits no replacement route, case, retry, larger research cap or Task B budget reuse.

[Execution 1.2](../../../../spec/paired-semantic-attempts-1.2.md) supervises the full batch and final
receipt under one monotonic deadline, including owned-process cleanup. The last second of a
600-second budget is reserved inside it for cleanup. Partial artifacts stay visibly incomplete;
complete hash-bound primary output can be replayed without research. Research is never annotation
input, fallback, consensus or veto.

[Execution identity](execution-identity.json) binds 34 runtime/schema/config files and a retained
snapshot, the existing explicit Python interpreter, dependencies and actual imported module paths.
No wheel was built or installed. [Expectations](expectations.json) remain evaluator-only and identical
to v1. [Evaluator](evaluate.py) keeps availability, host, exact contributor and support outcomes
separate, and treats partial consumption as unavailable. [Same-agent review](review.md) and
[probe plan](probe-plan.md) describe evidence and limitations. G3, G4 revision 2, G5 and blank target
remain separate zero-call controls; nested Continued remains an offline regression, not a live case.

Run [check_freeze.py](check_freeze.py) with the exact successor freeze and externally approved SHA-256
immediately before any eventual launch. It reads and compares; it never regenerates saved requests.
Every bound file, the complete execution inventory, interpreter/dependency identity and reconstructed
queries/four requests must match, and the reserved output must remain absent. Default readiness is
required; `--bindings-only` is diagnostic, never authorization. Any drift stops the launch.

The final digest and positive/negative checker receipts are stored separately under
`runs/semantic-citation-confirmation-2026-09-19/freeze-checks-v2/verification-receipt.json`; the receipt
is not an amendment to frozen members. No live inference follows preparation or a passing check.

Historical 0/4 complete frozen support, three research truncations and invalid research abstention
remain unchanged. These are exposed development cases, not holdout/statistical evidence or human/
domain acceptance. Saved-IR semantic acceptance and M2 remain open. No numerical compatibility or
downstream financial-isolation claim follows from header tests or empty outputs.
