# Nested presentation partition regression — reviewed expectations

The owner approved the independent implementation review's bounded offline correction. This
packet freezes that review's source transformation and additional boundary expectations before
runtime editing or candidate-answer testing. Reviewer: the continuing Codex agent, already exposed
to implementation, original fixtures and diagnostic outcomes; no new delegated or human/domain
review is claimed. All sources are authored development evidence. No inference is authorized.

Authority: docs/semantic-citation-implementation-review-2026-09-19.md Finding 1; operation 1.1's
presentation exemption and complete-partition rule; revised plan section 2.3. The literal
Continued wrapper says nothing about revenue membership. Source spans place it between the
recognized revenue category and the same complete recognition-timing/subtotal partition. Removing
that wrapper from membership counting must preserve both that partition and every literal occurrence.
This does not grant arbitrary wrappers a presentation role or infer a subtotal scope from geometry.

Expected sources (authored from the retained P1 source):

- nested: exact prior review source/IR bytes. Both outer total and subtotal are eligible. Outer
  contributors are subtotal plus lease; subtotal contributors are the two timing modes. Both answers
  must cite the revenue category. Continued is retained as optional support and is not a contributor.
- repeated: two consecutive Continued wrappers; same relationships and support requirements. Both
  presentation occurrences remain distinct, optional and present in the projection.
- reordered: swap the timing occurrences and replace body amounts with deliberately unbalanced
  values; same semantic labels contribute. New source identities, no amount-based selection.
- missing: blank over-time header; block both targets because group qualification fails.
- duplicate: two point-in-time labels; block the outer target because the subtotal scope is unknown.
  Do not claim that every possible subtotal-target query is blocked by this finite rule.
- extra: add a memo branch inside the revenue partition and widen the wrapper/group; block the
  outer target because the three-role summary no longer covers the immediate partition.
- unknown: replace Continued with Reported scope; block both targets, retaining unknown context.
- crossing: a revenue interval [0,3) and lower period interval [2,5) cross, with Continued occupying
  other recovered regions; block the outer target. Presentation exemption cannot repair crossing
  contextual groups.

Positive checks must reject omission of the category and all three subtotal-plus-component
selections for the outer target. They must reject selecting a recognized wrapper as a contributor.
A single-column display unit's broader contributor-domain question remains outside this correction.
Existing frozen S/G cases, prose gates and role isolation keep their original expectations.

Version/migration expectation: retain operation 1.1.0, prompt, schema, vocabulary and role policy;
bump the host analysis identity to header-source-analysis-1.0.1. Old queries/batches must fail
current binding checks. Preserve the complete pre-correction source package for offline old replay;
do not refresh old hashes or silently rescore old answers. Historical 0/4 support and research
failures are unchanged. G3–G5 remain local zero-call controls, never live model abstention tests.
