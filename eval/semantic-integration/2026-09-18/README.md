# Development saved-IR review, 2026-09-18

This evaluator-only directory is denied to runtime. The source packet was selected before
inference from the corrupted saved IR, never from clean-counterpart or injection answers.
The independent agent review freezes expectations with citations and its explicit limitations.
`pre-inference-freeze.json` reconciles those expectations unchanged, verifies the packet against
the selected IR, and declares the live bounds. Header relations are not numerical operand lists.

`offline-separation-tests.txt` records the focused implementation verification before live calls.
No human/domain approval, scored detector result or held-out accuracy is claimed.
