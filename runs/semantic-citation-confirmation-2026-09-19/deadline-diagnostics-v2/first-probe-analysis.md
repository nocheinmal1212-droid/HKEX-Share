The first run is retained in tests-first.txt and its uniquely named directories. Cancellation itself
returned inside every short ceiling. The observation harness used /bin/ps, denied by the filesystem
sandbox; it now uses kill(pid, 0) only on the fixture's recorded owned PIDs and requires ESRCH.
No escalation or wider process enumeration is needed. The first 8-second success fixture and
4-second slow-research fixture did not allow enough time for real selected-IR validation/consumption;
the cancellation path correctly interrupted those workers. Keep 3-second pure stage cancellation
tests, but give successful two-case lifecycle checks 60 seconds and primary-before-research/second
query checks 20 seconds. These are distinct test durations, not changes to the 600-second proposal.
The source work is real; fixture stalls now last 120 seconds and must be killed by the shorter
supervisor. No runtime assertion or production ceiling was widened to accommodate the harness.
