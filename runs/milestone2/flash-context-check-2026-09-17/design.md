# Flash context discrimination — frozen before inference

Owner authorized the selected next-test paragraph on 2026-09-17. Scope: four new scored calls, then its conditional continuation if context decisions are correct. This is fixture evaluation, not production selection or deployment. Preserve old runs, model designation and unknowns.

Core order is V4 Flash 0731 / S08, V4.1 Flash / S08, V4.1 Flash / S11, V4 Flash 0731 / S11. One fresh conversation each, no repeats, retries, response repair or automatic rerouting. This counterbalances order across cases, not across all time effects. All four planned calls run unless persistence/isolation fails. Two unavailable observations for one model end its work; no replacement call. Account-wide authentication/credit failures stop further stages.

Routes: exact `deepseek/deepseek-v4-flash-0731` through `deepinfra/fp8` (DeepInfra), and exact `deepseek/deepseek-v4.1-flash` through `together` (Together). Fresh official endpoint metadata is saved under metadata/. Advertised input/output USD per million: 0.06/0.18 and 0.30/1.20 respectively. These are the previously declared alternate routes, now frozen as primary routes for this new experiment. They differ from the prior experiment's OpenInference and DeepInfra routes, so before/after differences do not isolate a weight or generation effect. API-reported identities are checked, not taken as independent weight attestation.

Unchanged common explicit prompt, source payloads, output schema and evaluator expectations from model-selection-2026-09-17. Temperature 0; native default reasoning (no new effort override); 4,096 total completion tokens; 60-second deadlines; 131,072 response bytes; one concurrent call. Strict JSON schema and literal-ID host checks. Native supported parameters verified before calls. No budget tuning arm in this small test.

Source review before calls: S08 r04 refers to s06, year ended 2024, whereas r05 uses s02, 2025. A named direct relationship cannot reconcile that conflict: abstain, incompatible_context. S11 r04's s06 cannot allocate Group versus Company, whereas target r05 is Group: abstain, unknown_context. IDs must stay literal; no amount, computed answer or executable plan. Expectations remain agent-reviewed logical fixtures, not human-adjudicated real-report truth.

Advance each model independently only if both core calls have verified identity, valid contract, correct relation, correct reason and required support. If eligible, run exactly one each of S09 (currency conflict), S10 (presentation-basis conflict), H01 (unique linked Group distribution occurrence; select a-sub/b-dist), H02 (same key made ambiguous; abstain unknown_context). With two eligible models, alternate model order on successive cases. No new cases or changed labels. Freeze eligible settings before follow-up explanations. These four continuation cases are confirmation only: never used to tune settings. One repeat is not a reliability estimate. Complete all four bounded continuation cases unless persistence/isolation fails; do not replace failures.

Oracle: no fresh Gemini call in this four-call approval. Compare separately to the previously frozen Gemini screen and confirmation responses using the identical evidence/prompt; mark agreement as frozen-oracle replay, not contemporaneous availability or a new oracle sample. Independent source expectations determine correctness.

Explanations: same representative policy as the preceding run, declared now. For each model's core, sample earliest completed full-contract failure/refusal and earliest full-pass abstention, at most two distinct parents. For continuation, sample earliest completed full-contract failure/refusal, otherwise earliest full-pass abstention, at most one parent per model. Completed answers only; no explanations for timeout, empty/truncated or HTTP/transport failures. Report all eligible omissions and unavailable decisions. The first explanation request is the unchanged separate diagnostic instruction/schema: original task/evidence/response only, no expected answer or WA label, no alternate model output. A brief explanation with literal IDs and source excerpts, not private reasoning or replacement answer. Cap 2,048 tokens. Check citation item IDs and exact excerpts; diagnostic text never changes initial scores or enters later scored prompts. Preserve any diagnostic namespace limitations.

Absolute ceilings: 4 core + up to 8 continuation + up to 6 explanation = **18 inference calls**, **61,440 requested completion tokens**, **600,000 input-token planning allowance**, **USD 1 reserved at the frozen route rates**, **20 minutes of dispatch time**. Input token allowance is conservative planning, not local tokenizer attestation. Wire requests at most 32,000 UTF-8 bytes. Two public metadata GETs are counted separately. No exploratory calls beyond these stages. Missing reported costs remain unknown.

A new transport v3 records only fixed exception categories (DNS, TLS certificate/TLS, timeout, connection refusal/reset/abort, broken pipe, permission, HTTP read/protocol, other OS or unclassified), integer errno and fixed phase; it never records exception text, repr, URL, headers or credentials. Preserve HTTP status received before a body-read error. Tests must cover wrapped failures and ensure no credential text leaks. This classification indicates where an error surfaced, not who caused it.

Reuse the unchanged v2 dispatch/completion contract and read-only replay. New immutable directories and exact frozen request binding; `.env` bootstrap loads only OPENROUTER_API_KEY to worker environment. Worker reads of credentials, raw injection metadata, fixture inputs and evaluator files are denied and tested before paid calls. Runtime gets only explicit staged evidence, not evaluator keys, corpus globs, PDFs or counterpart data. No OCR or local hosting.

Report all-attempt and usable-answer denominators separately; useful correct abstention is a success. Distinguish unsupported selections, reason/support fidelity, refusal, malformed output, truncation, timeout, HTTP and classified no-HTTP errors. Full pass is not production correctness. Core 2/2 merely earns continuation; full 6/6 on this new route justifies further controlled development consideration, not promotion or a measured automatic cascade. No weighted winner or post-hoc threshold.
